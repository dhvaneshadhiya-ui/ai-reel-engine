# Questions — mac-multiple-headphones

1. Is PairPods a sponsor, partner, or paid placement? If yes, the reel and
   caption need a disclosure ("paid partnership" / "#ad"). The research found
   it to be an independent free open-source app by Paweł Woźniak; nothing in
   the script implies an affiliation.
2. The reel names Apple's built-in route (Audio MIDI Setup) before PairPods.
   That is deliberate — hiding a free built-in option would mislead — but it
   costs ~8 seconds. Keep it?

Judgement calls recorded, not questions:
- No date/version anchor in the hook: this is an evergreen how-to, not news.
- Low sentence-to-sentence bridging (check_script advice): the tutorial runs
  problem → built-in fix → its catch → the app → its catch; the order is the bridge.
- Defaults applied: opens on a slide with the presenter in a corner circle, ends
  on a CTA end card ("link in the pinned comment") with the presenter circle,
  music on, no on-screen credits.
