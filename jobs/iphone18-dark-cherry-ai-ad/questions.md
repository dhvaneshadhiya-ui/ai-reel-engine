# Open questions — iphone18-dark-cherry-ai-ad

1. **Showing the actual @kaye.creatives ad as b-roll.** The story is ABOUT
   their AI-generated Instagram Reel. Plan is to credit them on screen
   (name + handle, per RIGHTS) and show a few seconds of their ad, mobile-
   captured, as the visual proof for "look what this actually shows" --
   same as we'd screenshot any other creator's work we're reporting on.
   Okay to capture and use short clips of their Reel with on-screen credit,
   or would you rather we describe it via facecam + text only and not use
   their footage at all?

2. **CTA.** Script ends on a question CTA ("Think it can?") rather than a
   comment-keyword or follow CTA -- fits the "which color are you rooting
   for" energy of a reporting piece, not a lead-gen account. Fine as is,
   or want a different CTA type (e.g. explicit "comment your pick")?

3. **Register.** Tone is dry/skeptical (a fake ad "sold" the viewer, and
   that "should worry Apple") more than warm or hype. Planning the
   digital-twin avatar on `neutral` register (not `warm`). Confirm that
   reads right for this one.

4. **Runtime.** 207 words, ~74-79s at the locked voice speed -- inside the
   60-80s news band, no `allowLong` needed. Confirm that's the right
   length, or want it tighter/looser.
