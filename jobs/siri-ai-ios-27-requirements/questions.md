# Open questions — siri-ai-ios-27-requirements

1. **No CTA, and `new_job.py` scaffolded a comment-keyword giveaway
   ("comment SIRI...") in `giveaway.md` that I did not use.** This is a
   purely informational eligibility explainer with no actual resource to
   deliver by DM, so a comment-keyword CTA would be promising something
   that doesn't exist — exactly what `going-viral`'s adopted mechanics
   section says to avoid for `news`. The script ends on the payoff line
   instead, matching this channel's own precedent (`apple-october-lineup`
   and `ios-27-beta-8` both end on a thematic line with no CTA). Say the
   word if you'd rather I add a real CTA (e.g. "check Settings > Apple
   Intelligence & Siri and see which gates you clear" — a self-check
   prompt rather than a lead-gen one).

2. **Cut the alleged 12GB RAM requirement for Expressive Voices.** A
   search-engine summary claimed this, but neither of two full-article
   fetches (9to5Mac, Tom's Guide) that should have carried the figure
   actually contained it anywhere I could read. I could not independently
   confirm it, so it's not in the script at all — only the iPhone
   17 Pro/iPhone Air model restriction is spoken, which is corroborated by
   two outlets. Flag it if you have a source that confirms the RAM figure
   and want it added.

3. **Scoped strictly to "getting Siri AI," not "what Siri AI does."**
   The script never describes Siri AI's actual new capabilities
   (on-screen awareness, personal-context search, app actions) — only the
   four eligibility gates (phone, region, language, age). That matches the
   literal topic you gave me, but if you actually wanted a "what's new"
   reel instead of (or in addition to) an eligibility checklist, this is
   the wrong script for that and I'd write a second one.

4. **EU/Mac nuance simplified for pacing.** Apple's own post says Siri AI
   is unavailable in the EU "on iPhone or iPad" but IS available on Mac —
   the script just says the EU (along with China) "don't get it at launch
   at all," which is true for the phone story this reel is actually about
   but slightly overstates the EU picture if a viewer also uses a Mac.
   Say so if you want that carve-out spoken explicitly; it costs a few
   words I cut for pacing.
